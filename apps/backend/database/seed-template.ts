export const seedTemplate = `import type { Seed } from '../seeder';

export const up: Seeder = async ({ context }) => {
  // Do stuff
};

export const down: Seeder = async ({ context }) => {
  // Do stuff
};`;
